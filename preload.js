// L'app est 100% client-side (même code que la version web) et n'a besoin d'aucune API
// Node.js côté renderer. Ce preload reste vide par sécurité (contextIsolation activé,
// nodeIntegration désactivé) — à étoffer plus tard si des fonctionnalités natives
// (sauvegarde de fichier via dialogue système, etc.) sont ajoutées.
